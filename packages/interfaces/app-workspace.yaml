
apiVersion: kubocd.kubotal.io/v1alpha1
kind: ClusterInterface
metadata:
  name: app-workspace
spec:
  schema:
    properties:
      userNamespace: { type: string, required: true }
      sysNamespace: { type: string, required: true }


